pip install yfinance
pip install statsmodel
pip install matplotlib pandas numpy

All of these are required for the jupyter notebook to run successfully

Also keep in mind when running the ARIMA model, this will take time ~1-2 minutes per model.
This is due to the sheer size of the dataset and each date needing to be individually evaluated
